# secrets
.env
kaggle.json
*.key
*credentials*

# python
__pycache__/
*.pyc
.venv/
*.egg-info/

# data & runtime (large / regenerable)
data/historical/
data_cache/
reports/*/
!reports/backtests/
reports/backtests/*.csv

# generated dashboard payload (regenerated by `publish`)
site/data.json

# os
.DS_Store

# internal project guide — not for public repo
CLAUDE.md

# machine-specific real infra (only the .example belongs in the public repo)
deploy/cloudflared-worldcup.yml

# scraped squad data (regenerable via fetch_squads)
data/squads_wc2026.json
data/club_elo.json

# self-hosted player portraits (regenerable via 'portraits')
site/portraits/
data/portraits.json

# accumulating probability snapshots (movement tracking)
data/history.json
